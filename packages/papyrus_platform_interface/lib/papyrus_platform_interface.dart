/// Platform interface contracts and shared data models for the Papyrus WebView.
///
/// This library defines the abstract [PapyrusPlatform] interface, all shared
/// data transfer objects (requests, events, policies), and the full
/// configuration model used across every platform implementation.
library;

export 'src/models.dart';
export 'src/papyrus_platform.dart';
