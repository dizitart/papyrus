# papyrus_android

Android System WebView implementation package for Papyrus.

The native implementation maps `PapyrusSecurityPolicy`, navigation policy,
resource interception, downloads, permissions, console messages, renderer
process failures, mixed-content behavior, file access, and dark-mode settings to
Android WebView APIs.

Android uses the native Flutter platform-view path rather than the shared
desktop overlay model.

